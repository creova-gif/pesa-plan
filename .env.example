# PesaPlan environment variables
# Copy this file to .env and fill in your values.
# Both variables are required to enable cloud sync.
# If left empty the app runs in offline-only mode with no data loss.

# Get these from your Supabase project → Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
